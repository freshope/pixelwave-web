export default function TodayAlivePage() {
  return (
    <main className="wrap-wide">
      <section className="hero">
        <h1>오늘 하루</h1>
        <p className="lede">today-alive placeholder — Phase 1.3 에서 정적 랜딩 + /invite 이식.</p>
      </section>
    </main>
  );
}
